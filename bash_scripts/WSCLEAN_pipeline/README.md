Order to run files in:

all_inWSCLEAN1.sh - will remove point sources from measurement set giving the output of temp$idx.ms
wideband_multiscale - runs multiscale cleaning over a specificed bandwidth and creates a model for each frequency slice
all_inWSCLEAN2.sh - removed the multiscale model from the visiblities and images the bright source removed data for use in diffuse foreground removal
